import 'package:flutter/material.dart';


class Balence extends StatelessWidget {
  const Balence({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: EdgeInsets.all(10.0),
      color: Colors.amber,
      child: Center(
        child: Text("Balence  RS.0.0")
      ),
    );
  }
}
